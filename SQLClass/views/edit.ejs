<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>you are about this user:<%= user.email %></h2>
    <form method="post" action="/user/<%= user.id %>?_method=PATCH">
        <textarea name="username" id=""> <%= user.username %></textarea>
        <input type="password" name="password" placeholder="enter password ">
        <button>edit</button>
    </form>
</body>
</html>

<!-- to use patch req in form we put post or get as method but use a package called method-override.Lets you use HTTP verbs such as PUT or DELETE in places where the client doesn't support it. for this we install "npm i method-override"-->